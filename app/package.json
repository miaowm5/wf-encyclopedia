{
  "name": "wf-encyclopedia-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "unzipCdn": "for f in ./cdn/*.zip; do unzip -o \"$f\" -d ./resources/cdn; done",
    "buildApp": "npx neu update && npx neu build --release",
    "build": "npm run unzipCdn && npm run buildApp"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.0"
  },
  "dependencies": {}
}
