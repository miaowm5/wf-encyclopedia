{
  "name": "wf-encyclopedia-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "cleanDir": "rm -rf ./dist && rm -rf ./.tmp",
    "buildApp": "npx neu update && npx neu build --release",
    "build": "npm run cleanDir && npm run buildApp"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.0"
  }
}
